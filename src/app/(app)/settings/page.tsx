import Link from "next/link";
import { authSessionService } from "@/features/authentication/server/services/auth-session.service";
export default async function SettingsPage(){const {user}=await authSessionService.getDashboardContext();return <main className="page-shell max-w-3xl pb-24"><h1 className="text-4xl font-semibold">Hesap ayarları</h1><p className="mt-3 text-muted">Profilinizi, güvenliğinizi ve veri tercihlerinizi yönetin.</p><div className="mt-8 grid gap-4"><section className="panel p-6"><p className="text-sm text-muted">E-posta</p><p className="mt-1 font-semibold">{user.email}</p><p className="mt-4 text-sm text-muted">Kullanıcı adı ve profil bağlantılarınızı profil sayfasından güncelleyebilirsiniz.</p><Link className="mt-4 inline-flex rounded-full bg-accent px-4 py-2 text-sm font-semibold text-accent-foreground" href="/artist-profile/edit">Profili düzenle</Link></section><section className="panel p-6"><h2 className="font-semibold">Gizlilik ve hesap</h2><p className="mt-2 text-sm text-muted">Veri haklarınız ve hesap silme talepleri için destek ekibimize ulaşın.</p><Link className="mt-4 inline-flex rounded-full border border-line px-4 py-2 text-sm font-semibold" href="/contact">Destek talebi oluştur</Link></section></div></main>}
