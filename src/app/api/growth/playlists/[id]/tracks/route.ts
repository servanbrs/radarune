import { NextResponse } from "next/server";
import { getGrowthActor, growthJsonError } from "@/features/growth/server/http/growth-route";
import { socialService } from "@/features/growth/server/services/social.service";
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const actor = await getGrowthActor(); const body = await request.json() as { trackId?: string }; if (!body.trackId) return NextResponse.json({ error: "trackId gereklidir." }, { status: 400 }); return NextResponse.json(await socialService.addTrackToPlaylist(actor, (await params).id, body.trackId)); } catch (error) { return growthJsonError(error); } }
export async function DELETE(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const actor = await getGrowthActor(); const body = await request.json() as { trackId?: string }; if (!body.trackId) return NextResponse.json({ error: "trackId gereklidir." }, { status: 400 }); return NextResponse.json(await socialService.removeTrackFromPlaylist(actor, (await params).id, body.trackId)); } catch (error) { return growthJsonError(error); } }
