"use client";
import { useState } from "react";
export default function ContactPage() { const [sent,setSent]=useState(false); const [error,setError]=useState<string|null>(null); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError(null);const form=e.currentTarget;const r=await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});if(r.ok){setSent(true);form.reset();}else{setError((await r.json().catch(()=>null))?.error??'Mesaj gönderilemedi.');}} return <main className="page-shell pb-24"><div className="grid w-full gap-6 lg:grid-cols-[.8fr_1.2fr]"><section className="panel bg-[#111d20] p-8 text-white md:p-10"><p className="text-xs uppercase tracking-[.25em] text-[#44c7ad]">Radarune destek</p><h1 className="mt-5 text-5xl font-semibold">Birlikte çözelim.</h1><p className="mt-5 leading-8 text-white/65">Telif, dağıtım, ödeme veya hesabınızla ilgili her konuda bize yazın. Ekibimiz en kısa sürede dönüş yapar.</p><div className="mt-10 grid gap-3 text-sm text-white/60"><span>✦ Telif ve hak bildirimleri</span><span>✦ Teknik destek</span><span>✦ İş ortaklığı ve basın</span></div></section><section className="panel p-6 md:p-10"><h2 className="text-2xl font-semibold">İletişim formu</h2>{sent?<p className="mt-8 rounded-2xl bg-accent/10 p-4 text-accent">Mesajınız alındı. En kısa sürede dönüş yapacağız.</p>:<form onSubmit={submit} className="mt-6 grid gap-4"><input required name="name" placeholder="Ad soyad" className="rounded-xl border border-line bg-surface p-3"/><input required type="email" name="email" placeholder="E-posta" className="rounded-xl border border-line bg-surface p-3"/><input required name="subject" placeholder="Konu" className="rounded-xl border border-line bg-surface p-3"/><textarea required minLength={10} name="message" placeholder="Mesajınız" className="min-h-40 rounded-xl border border-line bg-surface p-3"/><button className="rounded-xl bg-accent px-5 py-3 font-semibold text-accent-foreground" type="submit">Mesajı gönder →</button>{error?<p className="text-sm text-danger">{error}</p>:null}</form>}</section></div></main>; }
