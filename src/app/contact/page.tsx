"use client";
import { useState } from "react";
export default function ContactPage() { const [sent,setSent]=useState(false); const [error,setError]=useState<string|null>(null); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError(null);const form=e.currentTarget;const r=await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});if(r.ok){setSent(true);form.reset();}else{setError((await r.json().catch(()=>null))?.error??'Mesaj gönderilemedi.');}} return <main className="page-shell max-w-2xl pb-24"><h1 className="text-4xl font-semibold">İletişim</h1><p className="mt-4 text-muted">Sorularınız veya telif bildirimleri için bize ulaşın.</p>{sent?<p className="mt-8 rounded-2xl bg-accent/10 p-4 text-accent">Mesajınız alındı. En kısa sürede dönüş yapacağız.</p>:<form onSubmit={submit} className="mt-8 grid gap-4"><input required name="name" placeholder="Ad soyad" className="rounded-xl border border-line bg-surface p-3"/><input required type="email" name="email" placeholder="E-posta" className="rounded-xl border border-line bg-surface p-3"/><input required name="subject" placeholder="Konu" className="rounded-xl border border-line bg-surface p-3"/><textarea required minLength={10} name="message" placeholder="Mesajınız" className="min-h-40 rounded-xl border border-line bg-surface p-3"/><button className="rounded-xl bg-foreground px-5 py-3 font-semibold text-white" type="submit">Gönder</button>{error?<p className="text-sm text-danger">{error}</p>:null}</form>}</main>; }
